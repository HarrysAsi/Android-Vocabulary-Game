<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/voc_quiz_splash</item>
    </style>

    <!-- Style for buttons in menu activity -->
    <style name="ButtonStyle">
        <item name="android:textSize">@dimen/text_main_size</item>
        <item name="android:textColor">@color/primary_text</item>
        <item name="android:gravity">fill</item>
        <item name="android:layout_height">@dimen/button_main_height</item>
        <item name="android:textAlignment">center</item>
        <item name="android:background">@drawable/menu_button_style</item>
        <item name="android:layout_marginRight">45dp</item>
        <item name="android:layout_marginLeft">45dp</item>
        <item name="android:layout_marginTop">20dp</item>
    </style>
    <!-- Style for TextViews in game activity -->
    <style name="TextViewGameActivity1">
        <item name="android:textStyle">bold</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:textAlignment">center</item>
    </style>
    <!-- Style for Buttons in game activity -->
    <style name="ButtonStyleInGame">
        <item name="android:layout_marginLeft">@dimen/margin_20</item>
        <item name="android:layout_marginRight">@dimen/margin_20</item>\
        <item name="android:layout_marginTop">@dimen/margin_10</item>
        <item name="android:background">@drawable/in_game_button_style</item>
    </style>
    <!-- Style for TextViews in game activity -->
    <style name="TextViewStyleInGame">
        <item name="android:layout_margin"> 2dp </item>
        <item name="android:gravity"> center </item>
        <item name="android:textAlignment"> center </item>
        <item name="android:visibility"> invisible </item>
        <item name="android:textSize"> @dimen/text_main_size_25</item>
    </style>


</resources>
